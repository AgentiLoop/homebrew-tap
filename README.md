# AgentiLoop Homebrew Tap

```sh
brew tap agentiloop/tap
brew install --cask agent
```

Installs [Agent!](https://github.com/AgentiLoop/Agent) — a native macOS autonomous agent — into `/Applications/Agent!.app`.

Requires macOS 26 (Tahoe) on Apple silicon. Update with `brew upgrade --cask agent`.
